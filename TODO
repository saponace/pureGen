Continue MeshedWorld class (creating meshes for each visible block)

Remember to :
    share materials. 1 material per block type (grass, water, ...)
    create chunks




(use GeometryBatchFactory.optimize(chunk), on every chunk (merge geometries withe the same material))
